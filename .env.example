# --- DATABASE CREDENTIALS ---
# Administrative password for Oracle (SYS, SYSTEM, PDBADMIN)
ORACLE_PWD=replace_with_oracle_admin_password

# --- SECURE TELEMETRY ---
# AES-256 Key for reading value encryption (Base64)
READING_VALUE_KEY=replace_with_base64_aes_key

# --- SERVICE ENDPOINTS ---
KAFKA_BOOTSTRAP_SERVERS=localhost:9092
VAULT_ADDR=http://localhost:8200
VAULT_TOKEN=REDACTED_VAULT_TOKEN
